// Centralized defaults for wproj runtime when no external config file is used.

pub const DEFAULT_WORK_ROOT: &str = ".";
pub const DEFAULT_ANALYSE_MODE: &str = "i"; // interactive (kept for completeness)
pub const DEFAULT_ANALYSE_LINE_MAX: &str = "3";

// kept minimal; handlers set logging explicitly where needed
